import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  overviewContainer: {
    flexDirection: "row",
    padding: 12,
    gap: 12,
    borderRadius: 10,
    borderWidth: 1,
  },
  text: {
    fontSize: 18,
    fontWeight: 600,
  },
});
